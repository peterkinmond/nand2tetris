/** Initializes a new Hangman game and starts running it. */
class Main {
  function void main() {
    var HangmanGame game;
    let game = HangmanGame.new();
    do game.run();
    // TODO: dispose of game
    return;
  }
}
